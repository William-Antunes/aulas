const nomes = ['Ana', 'Ju', 'Leo', 'Paula'];

// nomes.splice (indice, delete, elem1, elem2, elem3)
// nomes.splice(1, 0, 'Rodrigo') adiciona o elemento 'Rodrigo' no indice 1, sem remover ninguem
// nomes.splice(2, 1, 'Fernando') remove 1 elemento do indice 2 e adiciona 'Fernando' no lugar
// nomes.splice(3, 2) remove 2 elementos a partir do indice 3
// console.log(nomes);

// pop = nomes.splice (-1, 1)

// shift = nomes.splice (0, 1)

//push = nomes.splice (nomes.lenght, 0, 'NovoNome')

// unshift = nomes.splice (0, 0, 'NovoNome')
